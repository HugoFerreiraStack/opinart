import 'package:flutter/material.dart';

class DefaultPaginationIndicator extends StatelessWidget {
  const DefaultPaginationIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [Text("")],
    );
  }
}
